# Root folders to scan. Separate multiple folders with ":".
PHOTO_ROOTS=/Users/you/Pictures/Lightroom Saved Photos

# Where the index and thumbnail cache live (default: <repo>/.cache)
# LD_CACHE_DIR=/Volumes/SSD/latent-darkroom-cache

# API port
PORT=5174
