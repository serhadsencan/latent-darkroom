{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --no-warnings=ExperimentalWarning --experimental-strip-types src/index.ts",
    "start": "node --no-warnings=ExperimentalWarning --experimental-strip-types src/index.ts",
    "scan": "node --no-warnings=ExperimentalWarning --experimental-strip-types src/cli-scan.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "exifr": "^7.1.3",
    "fastify": "^5.2.1",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
